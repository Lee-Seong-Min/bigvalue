<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.bigvalue.mapper.TestMapper">

    <select id="getYearMonth" resultType="int">
        SELECT
            year_mon
        FROM
            sync
                LIMIT 1
    </select>

</mapper>